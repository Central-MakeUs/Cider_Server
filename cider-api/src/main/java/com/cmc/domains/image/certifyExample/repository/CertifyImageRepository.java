package com.cmc.domains.image.certifyExample.repository;

import com.cmc.image.certify.CertifyImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CertifyImageRepository extends JpaRepository<CertifyImage, Long> {
}
